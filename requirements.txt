# ──────────────────────────────────────────────────
#  Reference Section Quality Pipeline
#  Python >= 3.9
# ──────────────────────────────────────────────────

# HTTP client — used by pipeline.py, reference_parser.py, checks/check_doi.py
requests>=2.31.0

# Progress bars — used by reference_parser.py batch mode
tqdm>=4.66.0

# XML parsing — used by reference_parser.py to parse GROBID TEI responses
lxml>=5.1.0

# ── API service (api.py) ──────────────────────────

# Web framework — REST API layer wrapping pipeline.run()
fastapi>=0.110.0

# ASGI server — serves the FastAPI app
uvicorn[standard]>=0.29.0

# Pydantic v2 — bundled with FastAPI; listed explicitly for clarity
pydantic>=2.0.0
