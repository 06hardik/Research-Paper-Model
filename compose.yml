services:
  reference-section-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: reference-section-api
    env_file:
      - .env
    environment:
      API_PORT: ${API_PORT:-8000}
    ports:
      - "${API_PORT:-8000}:${API_PORT:-8000}"
    restart: unless-stopped
